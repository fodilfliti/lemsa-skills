name: lemsa_lab
description: Internal Lemsa architecture sandbox — Task Tracker proof app.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^3.0.3
  json_annotation: ^4.9.0
  loading_indicator: ^4.0.2
  redact: ^0.1.0
  dio: ^5.11.0
  flutter_scale_kit: ^2.0.1
  flutter_scale_theme_kit: ^1.0.1
  auto_route: ^9.3.0
  slang_flutter: ^4.19.0
  drift: ^2.28.0
  drift_flutter: ^0.2.4
  sqlite3_flutter_libs: ^0.5.0
  path: ^1.9.1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  slang: ^4.19.0
  auto_route_generator: ^9.3.1
  build_runner: ^2.4.15
  drift_dev: ^2.28.0

flutter:
  uses-material-design: true
